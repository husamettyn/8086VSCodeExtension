STG SEGMENT PARA STACK 'ST_'
	DW 30 DUP(?)
STG ENDS

DSG SEGMENT PARA 'DS_'
	TABAN1 DW 4
	US1 DW 3
	TABAN2 DW 3
	US2 DW 4
	SONUC1 DW 0
	SONUC2 DW 0
DSG ENDS

CSG SEGMENT PARA 'CS_'
	ASSUME CS: CSG, DS: DSG, SS: STG
	MAIN PROC FAR
		PUSH DS
		XOR AX, AX
		PUSH AX 				; GERI DONUS'U BELIRTMEK ICIN

		MOV AX, DSG
		MOV DS, AX 				; DS'I ATAMAK ISTIYORUZ O YUZDEN
		MOV CX, US1
		MOV BX, TABAN1
		CALL US_AL
		MOV SONUC1, AX


		RETF
	MAIN ENDP
	
	
	US_AL PROC NEAR
		;AX=1, BX = TABAN1, CX = US1
		MOV AX, 1
		L1: 
			MUL BX
		LOOP L1

		RET
		

	US_AL ENDP

CSG ENDS
		END MAIN
